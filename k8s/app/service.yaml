apiVersion: v1
kind: Service
metadata:
  name: self-heal-app-service
  labels:
    app: self-heal-app
spec:
  selector:
    app: self-heal-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
